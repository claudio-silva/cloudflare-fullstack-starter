# ====================================================================
# Cloudflare Fullstack Starter - Environment Variables (example)
# ====================================================================
# Copy to .env.local, .env.preview, or .env.production and fill values.
# Local defaults are ready to run out-of-the-box.
# ====================================================================

# --------------------------------------------------------------------
# Core runtime
# --------------------------------------------------------------------
ENVIRONMENT=local

# Email (Resend) - required for signup verification in non-mock flows
RESEND_API_KEY=

# --------------------------------------------------------------------
# CLI admin (only needed for preview/production)
# --------------------------------------------------------------------
CLI_ADMIN_EMAIL=
CLI_ADMIN_PASSWORD=

# --------------------------------------------------------------------
# CLI API URL overrides (optional)
# Defaults:
#   local:    http://localhost:5173
#   preview:  https://preview.example.com
#   prod:     https://app.example.com
# --------------------------------------------------------------------
CLI_API_URL=http://localhost:5173
CLI_API_URL_PREVIEW=https://preview.example.com
CLI_API_URL_PRODUCTION=https://app.example.com

# --------------------------------------------------------------------
# CLI testing (optional)
# --------------------------------------------------------------------
CLI_MOCK=false
